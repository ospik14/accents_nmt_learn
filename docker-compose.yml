services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DATA_DIR: /app/data
      AUTO_SEED: "1"
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
