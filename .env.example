BOT_TOKEN=

# SQLite (default in Docker via volume /app/data)
# DATABASE_URL=sqlite+aiosqlite:////app/data/bot.db

# PostgreSQL (use with docker compose -f docker-compose.yml -f docker-compose.postgres.yml up -d)
# POSTGRES_USER=nmt
# POSTGRES_PASSWORD=change_me
# POSTGRES_DB=nmt_bot
# DATABASE_URL=postgresql+asyncpg://nmt:change_me@db:5432/nmt_bot

AUTO_SEED=1
